interface:
  display_name: "Secure Code Review"
  short_description: "Find exploitable flaws introduced by a diff"
  default_prompt: "Use $secure-code-review to review this change for supported security findings."
