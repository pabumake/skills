# Coding

The active engineering workflow comes from the pinned `mattpocock/skills` baseline installed by the repository root `install.sh`. This directory no longer contains promoted skill copies.

See the root catalog for the exact upstream commit and 18-skill allowlist. The previous adaptations remain under `skills/deprecated/mattpocock-adaptations/` for history and are not installed.
